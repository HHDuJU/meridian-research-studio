# Order I1 for Grok Build (prepared 23 September 2026; approved; not yet sent)

Status: the investigator approved this order on 23 September, with its 150-call cap. Grok's weekly usage limit is reached until 13:46 UTC on 28 September, so the integration owner's scheduled follow-up pastes the text below into the Grok Build thread "AI-Powered Medical Research Software" as one message at 14:15 UTC that day, after checking that GitHub `main` and `claude/meridian-integration-a45` are on the same commit and that this text and `docs/HANDOFF.md` section 4 name that tree's test count and digest. One coordinator sends orders to Grok at a time.

---

Order I1 from the integration owner (the Claude orchestrator session in the investigator's Cowork workspace). The coordinating Fable session has ended; only this session sends you orders. The investigator approved this order. Roles from now on: Claude writes and integrates Meridian's code and runs the replay bank; you run live plans with Grok 4.5 and host the preview; you change no product code unless an order says so.

Receipt for a45: verified on the bytes. Tree d355530b... and results cf8e6d6f... equal their sidecars; verify-archive 2,137 of 2,137; install, typecheck and build pass; 229 of 229 tests; samples 3 of 3 and bank 103 of 103 qualifying in ui mode, identical to your run scenario by scenario (17,918 checkpoints, 2,136 screen actions, treeSha256 877d7256...). Counts on the a45 bytes: authored 106, executed attempts 106, qualifying 106. Every expected-result file equals the coordinator's A4.5 packet. Accepted.

New trunk: a45 merged with Claude's integrity branch (live PubMed, OpenAlex, ClinicalTrials.gov and Crossref retrieval through server functions with a per-host request gate; investigator-only local facts; whole-record appraisal in batches; S1 re-run at acceptance; fixes from an independent review; an investigator action to mark a gate not required for a decision), plus D10, which is done: a model never sets a gate (a model "met" is a proposal the investigator confirms on screen); every decision is ready to act on only after the investigator records its research ethics status (approved with the reference, or not required with the reason); an approval the investigator's own facts leave open blocks until the investigator acts on it; the model's statements about approvals are advisory. Do not re-implement or change D10. Scenario format 1.3 with validator 1.4; 97 bank scenarios and 2 samples were amended, each keeping its previous version in original-v<n>/. Your support.ts and numbers.ts are unchanged and remain the only claim-support authority. Source: https://github.com/HHDuJU/meridian-research-studio, branch main (the same commit as claude/meridian-integration-a45; the D10 code is commit f1dbc9a, later commits change only docs/). docs/HANDOFF.md lists every change and check; docs/amendments/WORK_ORDER_2_AMENDMENTS_D10_2026-09-23.md explains D10.

1. Keep the current a45 workspace as a copy (for example /workspace-a45, including public/artifacts); delete nothing.
2. Make the head of main the /workspace tree, keeping your .grok/, the startup.sh contract and public/artifacts/. Run npm ci, npm run typecheck, npm run test:lib (expect 294 of 294), node scripts/tree-digest.mjs . (expect treeSha256 317e1307aca646ef184c4e3b94237d6f6b78bd15f7ae9cd14e09b4cbd51d3805), node scenarios/validate-scenarios.mjs scenarios/bank-v1 and the same for scenarios/bank-samples (expect both to pass), then the three samples in ui mode as before (expect 3 of 3 qualifying). The whole bank was run in ui mode on these bytes by the integration owner on 23 September (103 of 103 qualifying, 19,557 checkpoints, 2,366 screen actions, none off the screen route); do not rerun it, to keep your weekly usage for step 3.
3. Live run with the real model: start the app in live mode (npm run dev, no scenario mode, XAI_API_KEY from your environment). Run npx tsx scripts/live-e2e.mjs scenarios/live/warmup.json results/live/warmup --base-url http://127.0.0.1:8080 --model-wait-min 10, then the same command, one plan at a time, for live-a-ketamine, live-b-eras, live-c-opioid-night, live-d-gastric-us, trial-a-facet-rfa-threshold, trial-b-genicular-rfa, trial-c-esp-rib-fracture and trial-d-rfa-prom-followup (output results/live/<plan>). Limits: at most 150 model calls in total; stop and report if reached. If XAI_API_KEY is missing, or PubMed, OpenAlex, ClinicalTrials.gov or Crossref cannot be reached from the sandbox, report that part as blocked; never mock a model reply or a registry response. A failed step is a result: record it and continue with the next plan; do not change code, tests, plans or expected results during this order. Expected under D10: no plan records an ethics status, so each decision's action status stays "blocked" with the blocker that the investigator has not recorded the research ethics status of this work, and gates the model calls met stay "unknown" with a proposal; Accept still selects a decision whose cited claims S1 supports. Report these as they are; they are not defects.
4. Deliver meridian-i1-results.tar.gz through the /artifacts .bin route with a .sha256 sidecar: the samples COUNTS.json and summary.jsonl, and for each live plan report.json, study-final.json and export.json with every abstract, contentVersions and documents object replaced by its sha256 and length (no publisher text, no screenshots). Reply with the routes, sha256 and byte counts, the samples counts, the total model calls and tokens from the studies' usage records, and one line per plan: steps passed or failed, the decision kind, the accept result, the action status and its first blocker.
5. Then hold: no edits until the next order. Boundaries unchanged: no push, no spending beyond the call cap, synthetic scenario data only, held-out cases E and F untouched.
