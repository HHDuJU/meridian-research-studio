# Order I1 for Grok Build (prepared 23 September 2026; not yet sent)

Status: written by the integration owner (Claude orchestrator session) and held back. Sending it spends the investigator's xAI quota (the live run calls Grok 4.5), so it goes out only after his explicit approval of that spending. Paste the text below into the Grok Build thread "AI-Powered Medical Research Software" as one message. One coordinator sends orders to Grok at a time.

---

Order I1 from the integration owner (the Claude Opus orchestrator session). The coordinating Fable session has ended; from now on only this session sends you orders. The investigator approved this order.

Receipt for a45: verified on the bytes. Tree d355530b... and results cf8e6d6f... equal their sidecars; verify-archive 2,137 of 2,137; install, typecheck and build pass; 229 of 229 tests; samples 3 of 3 and bank 103 of 103 qualifying in ui mode, identical to your run scenario by scenario (17,918 checkpoints, 2,136 screen actions, treeSha256 877d7256...). Counts on the a45 bytes: authored 106, executed attempts 106, qualifying 106. Every golden equals the coordinator's A4.5 packet. Accepted.

New trunk: a45 merged with Claude's integrity branch (live PubMed, OpenAlex, ClinicalTrials.gov and Crossref retrieval through server functions with a per-host request gate; investigator-only local facts; model gates grounded in investigator text; whole-record appraisal in batches; S1 re-run at acceptance; fixes from an independent review; an investigator action to mark a gate not required for a decision). Your support.ts and numbers.ts are unchanged and remain the only claim-support authority. Source: https://github.com/HHDuJU/meridian-research-studio, branch main (same commit as claude/meridian-integration-a45). docs/HANDOFF.md lists every change, the checks and the expected pinned treeSha256.

1. Keep the current a45 workspace as a copy (for example /workspace-a45, including public/artifacts); delete nothing.
2. Make the head of main the /workspace tree, keeping your .grok/, the startup.sh contract and public/artifacts/. Run npm ci, npm run typecheck, npm run test:lib (expect the count in HANDOFF section 4), node scripts/tree-digest.mjs . (expect the digest in HANDOFF section 4), then samples and the whole bank in ui mode as before (expect 3 of 3 and 103 of 103).
3. Live run with the real model: start the app in live mode (npm run dev, no scenario mode, XAI_API_KEY from your environment). Run npx tsx scripts/live-e2e.mjs scenarios/live/warmup.json results/live/warmup --base-url http://127.0.0.1:8080 --model-wait-min 10, then the same command, one plan at a time, for live-a-ketamine, live-b-eras, live-c-opioid-night, live-d-gastric-us, trial-a-facet-rfa-threshold, trial-b-genicular-rfa, trial-c-esp-rib-fracture and trial-d-rfa-prom-followup (output results/live/<plan>). Limits: at most 150 model calls in total; stop and report if reached. If XAI_API_KEY is missing, or PubMed, OpenAlex, ClinicalTrials.gov or Crossref cannot be reached from the sandbox, report that part as blocked; never mock a model reply or a registry response. A failed step is a result: record it and continue with the next plan; do not change code, tests, plans or goldens during this order.
4. Deliver meridian-i1-results.tar.gz through the /artifacts .bin route with a .sha256 sidecar: the bank and samples COUNTS.json and summary.jsonl, and for each live plan report.json, study-final.json and export.json with every abstract, contentVersions and documents object replaced by its sha256 and length (no publisher text, no screenshots). Reply with the routes, sha256 and byte counts, the three counts, the total model calls and tokens from the studies' usage records, and one line per plan: steps passed or failed, the decision kind and the accept result.
5. Then hold: no D10 and no edits until the next order. Boundaries unchanged: no push, no spending beyond the call cap, synthetic scenario data only, held-out cases E and F untouched.
