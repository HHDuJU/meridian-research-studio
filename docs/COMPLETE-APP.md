# Complete application status

The live preview in Grok Build runs the sandbox working tree. That tree has no git commit.
GitHub `main` is `e27921e1d0795b1c0627956578bf8b5294261bd0` (library export only).
This branch `app/complete-v1` is where the remaining app shell is being added: `package.json`, routes, UI, `src/lib/seed.ts`, `src/lib/guidelines.ts`, Vite/Nitro host files.

Do not merge `cowork/evidence-integrity-v1` here until D7 is fixed and Vito approves.
