# Deliverable 1b — L1 library integration (private)

No push, no PR, no merge. Builds on phases 1–2 archive SHA-256
`19810a0dd49d0371391b515d68465ad2d0628fa840025fe6a71400c55651c0a9`.

Receipt of that archive (2026-09-22T11:20Z) plus A3 L1.1–L1.6 and D20(a)–(d), D21, D22, P1, P2, R1, S17.

## What this archive does

- Integrates the 875fe59 library so Illuminate goes through `applyAiResult` (study as 4th argument) and `mergeStage`.
- Six boundary corrections L1.1–L1.6.
- D20(a): empty-search confirmation is an investigator screen action; model JSON cannot complete an empty scan.
- D20(b): schema-3 empty scan migration clears certainty and completion; historical GRADE kept as `unsupportedGradeOverall` stale.
- D20(c): omitted grade over zero records resolves to unassessed.
- D20(d): model-only leads cannot receive overall certainty.
- D21: loading until persist hydration; missing only after.
- D22: one Export JSON click, one download dispatch.
- P1/P2 packaging; R1 replay/record; S17 runner on three sample scenarios.
- `npm test` = `tsx --test tests/*.test.ts` (`test:lib`). `test:platform` is separate.

## Tests

Command: `npm test` / `npm run test:lib` (`tsx --test tests/*.test.ts`).

## Not waiting

A3.2 (2026-09-22T11:58Z): D20 contract corrected (leads-only incomplete, no certainty without retrieval); D23–D28; scenario format 1.1. A3 sample oracles withdrawn.
