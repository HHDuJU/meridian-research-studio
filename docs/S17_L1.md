# S17 sample run on this L1 tree (store mode)

Command: `npx tsx scripts/run-scenarios.mjs scenarios/bank-samples results/l1-store-20260922 --mode store`

| Sample | Result | Notes |
|---|---|---|
| sc-000-level1 | 154 checks, 2 fail | `store.scan.gradeOverall` expected `low`, observed `""`; `issues.count` expected 1, observed 2. D20(d): certainty over uninspected model leads is not assignable. Not weakened. |
| sc-000-level3 | 157 checks, 12 fail | S11 `selectionStatus`/`actionStatus`/`design.basis` capability absent, recorded as such. |
| sc-000-level4 | 148 checks, 14 fail | S11 absent; decision `status` stays `accepted` after source change (stale-decision is phase 5 / D3). |

UI mode: not run in this archive (no Chromium drive of Illuminate). Status S17 TESTED for store mode, not END-TO-END.
