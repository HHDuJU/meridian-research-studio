# Failed attempts (kept)

- Date variant without typed dates used to verify (2019 vs 2022 same DOI/title; 2018 vs issued 2017 McCormick). Receipt 2026-09-22: unresolved unless source-bound typed dates corroborate. Positive control: 2021-online / 2024-print with PMID 34624374 evidence.
- `compareWithRegistry` treated empty normalized titles as exact (`"" === ""`), so 麻醉镇痛 vs 心脏疾病 and two empty titles verified. Unicode-aware comparison now required.
- `applyLookupOutcome` wrote `result: "match"` when `cmp.dateVariant` was set even if `cmp.result` was mismatch (unrelated title + DOI year gap).
- Scan completion survived an invalidated empty-search confirmation (school-heat then marine-ecology Illuminate). Actor-only flags also kept completion through schema-4 migration.
- Same-version persist merge repaired studies and overwrote localStorage without a byte backup; quota failure still wrote the new main value.
- Runtime `VITE_SCENARIO_MODE=true` plus `MERIDIAN_MODEL_MODE=replay` entered replay on a production build that had no scenario-key field.
- A3.3 runner diagnostics (independent receipt, 2026-09-22): UI steps with `expect.issues` / `expect.error` skipped those assertions; `{min:99999}` issue counts and required refusal text PASSed; reload/reopen PASSed from memory after a storage that discarded every write; FAIL runs exited 0; `result.json` was written before `export.json` existed so `result.artifacts` omitted the file. Do not restore those skips. Equality is recursive JSON with object-key order ignored only — do not drop `documents` / `repairsApplied` / `migrationEvents` / `migrationBackupHash` / `claim.origin` to force `export.equalsStore`.
- Abstract `change-source` that leaves the stored field unchanged must not assert staleness. UI `change-source` remains unsupported.
- sc-000-level3 version 2 expected a model `constraints: null` to store `problem.constraints` as `""` with issue `cleared` and later told the investigator to re-enter them. `applyAiResult` did that for null, empty string, and invented replacement. Investigator constraints are not model-editable. Version 2 kept as `bank-samples/original-v2/sc-000-level3.json`.
- `createGuardedStorage` treated any existing `backup.4.*` key as proof without reading it. A `"not json"` or empty-study snapshot then let a repair overwrite MAIN_KEY that held newer investigator notes; the original survived nowhere and no failure was recorded. A backup proves recoverable bytes only by equality with the current original.
- D25 schema-4 rehydrate (receipt 0a1b0136, 2026-09-22 13:18 UTC): `isConsequentialReconciliation` returned false when migrateStudy added `documents:[]` and `claim.origin:"unknown"` with no new `repairsApplied` id, so the next persist destroyed the pre-repair raw. The same path with a corrupt `backup.4.*` and a quota failure on a new copy overwrote MAIN_KEY because the backup/refusal branch never ran. Marked-repair, stale-backup, and quota cases had already passed. Ordinary autosaves must stay backup-free.
- G11 (receipt 0a1b0136): stage-frame showed issues only when `!result.ok`. A model null/empty/replacement of investigator constraints applied the statement (`ok: true`) and kept the stored constraints, but the dropped issue was invisible in the browser. Partial-apply must show path and message without rejecting allowed fields.
- D16/F3 increment (2026-09-22): empty-string overwrite of `ids.ts`, `records.ts`, and `fulltext.ts` during patching. Restored `ids.ts` and `records.ts` from `/tmp/meridian-before/` exact copies and rewrote `fulltext.ts` from the inspected spec; `decision.ts` `uid` import was restored the same way. A later live phase-3 pass overwrote `ids.ts` and `records.ts` again and restored them from before-copies. Do not reconstruct those files from memory. Do not use a whole-file rewrite or a truncating patch to save tool calls.
- contracts.ts `isRecord` header was eaten (2026-09-22) by a replacement whose old_string was only `export function isRecord...` and whose new_string was a new helper. Restored the `isRecord` function immediately. A too-short old_string that deletes a following export is a failed attempt; keep it listed.
- F3 `treatAsFullTextRead` (receipt fbcf7c08): returned true when `documents=[]` if `fullTextRead.documentId` was set; when a document matched id but `recordId` was `ev-other` and `sourceScope` was abstract; when a different document id shared the item `recordId` (fallback to an unrelated version); and when `complete: true` with `sections=[{id:s1,read:false}]`. Absence of documents is never proof. Keep the matching stored full-text positive.
- C1 (combined 2d1db519): `sourceManifest` included 71 generated files under `.vercel/output`; 21 changed during a concurrent build and the pinned-digest test failed. `.vercel` is now a skipped directory. A source-file change still changes the digest; a generated-output change does not.
- S13 identity-gate-on-grade (combined 2d1db519 / A4 samples): `applyAppraisal` required `provenance.checks.length` before applying a grade and logged `appraisal refused: no identity attempt`. Retrieved records from `ingestRecords` have `checks: []`, so item grades stayed `unrated` and Scan Illuminate added 14 issues. Samples require grades, claims and certainty over retrieved records with `issues.count` 0. Registry identity checks still cannot set provenance or verification; that is not a grade gate.
- A4.1 R-1 on 2d1db519: `scripts/run-scenarios.mjs` called `spawn` with no `node:child_process` import (`ReferenceError` at the self-start path). Owner runs used `--base-url`. The import is required and a control must start the server once.
- A4.1 R-5: `treeSha256` included `scenarios/`, so installing the bank moved the digest (95cca131... vs manifest 839bcada...). Digest skips `scenarios/` and `results/`; the pack walk still copies them.
- D29 (A4.2): `markUnknownIds` rematched `claim-9` inside `⟦unresolved:claim-9⟧` because `:` is a non-word boundary. The second Design Illuminate nested the marker (`⟦unresolved:⟦unresolved:claim-9⟧⟧`) and logged two `unresolved-reference` issues for text the model did not send. Level 4 golden `issues.count` 0 stands. Marking runs once; an already-marked reference is left as it is.
- R-6 (A4 bytes bank): `page.waitForEvent("download")` was created before the click and not always awaited; a timeout became an unhandled rejection and aborted the whole bank after 31 scenarios with no summary. A step error is that scenario's FAIL checkpoint; the run continues; the summary is always written.
- T-3 (A4.1): `applyAiResult("design")` wrote `studyPatch.family` from a proposed `recommended`, so sc-012 family left null until acceptance. A proposed recommendation never changes `family`; `acceptDecision` does, from `recommendedFamily`.
- T-4: `applyDecision` filled `recommendedFamily` from `study.family` / `design.recommended` when the payload omitted it, so sc-005 qualitative became implementation and sc-019 rct became pragmatic-trial. The model's recommended family is stored; a classifier of the need does not overwrite it.
- T-5: a later discovery `items` payload wrote `gradeOverall: ""` and cleared an appraisal grade (sc-011 moderate). Discovery never assigns certainty and does not clear an appraisal over retrieved records.
- T-6 sc-063: `decisionIsSupported` treated `implementation` with empty claims as a committing kind and refused accept. Implementation with empty claims is acceptable as a selection; unknown gates keep `actionStatus` blocked. sc-021 first accept of a pursue that still rests on ledger claim `c2` (after unknown `c9` was dropped) remains allowed; the golden that expected a refusal is a golden error.
- T-2 remaining (A4.2, a42-bank-ui 18:22): `set-field path=family` called `update({family})` and did not write `design.basis`. sc-018 step 3 expected explicit after the investigator chose cross-sectional; observed unresolved. `setFamily` now records explicit (cleared → unresolved). Design Illuminate no longer writes `basis`. Accept sets explicit only when a family is applied (`decision.recommendedFamily` or `design.recommended`); sc-028 with no family stays unresolved.
- T-4 leftover: omitted `recommendedFamily` was stored as `""`, so goldens expecting `isNull` failed. The field is now absent when the model omitted it.
- D8 (phase 4): `applyAppraisal` stripped unknown `sourceIds` from a source-derived claim and left the claim on the active ledger (`appraise.test.ts` expected `claims.length` 3 and `sourceIds: []`). The raw claim now stays in `scan.quarantine.claims` with those ids intact and an `unsupported` issue at `claims[id]`; `ledgerIssues` / `decisionIsSupported` block relying action. A quoted passage is still not rewritten (D16), including when the claim is quarantined.
- D8 sample false positive (2026-09-22T19:24Z / 19:26Z): extracting every English number word (`one`, `three`) from annotation limitations quarantined "one hospital" and "one fire service" and made sc-000-level1 `issues.count` 1 then 2. Clock times (`19:00`) also leaked as 19. Number-word extraction for counts is now 11+ and hyphenated tens only; `\d{1,2}:\d{2}` is masked; 48-hour duration still quarantines.

