The main gaps are the lack of a completed-test outcome, the risk of duplicate testing and the uncertainty about whether the rollout schedule is related to performance. The landmark FEEDBACK-GP trial already shows that dashboards work, so the main remaining gap is local uptake. I can list these as structured gap items with severity ratings if you would like.